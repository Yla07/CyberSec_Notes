It enables low latency data transmission between internet applications which makes it perfect for real time applications where low latency is important but some data loss is acceptable for example streaming and online gaming.

unlike the [[TCP]] , UDP is connection-less and doesn't wait for all packets to arrive. It transmits even if all packets haven't arrived
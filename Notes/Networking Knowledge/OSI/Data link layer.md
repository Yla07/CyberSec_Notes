(DLL) 
It is responsible for node to node delivery of the message. main function of this layer is transfer data error-free from one node to physical layer.

When a packet arrives in a network, it is the responsibility of the DLL to transmit it to the Host using its [MAC address](Terminology). Packet in the Data Link layer is referred to as Frame.
